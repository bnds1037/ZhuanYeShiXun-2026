## 🛠 文件二：Technical_Standards.md（团队技术规范文档）

```markdown
# 《发丘绝响：大秦妖冢》团队技术与代码规范文档

## 1. 版本控制与 Git 工作流
为了确保多人协作下项目的稳定，全员必须严格遵守以下 Git 提交规范：

### 1.1 分支管理
* `main` 分支：主生产分支，保持随时可发布的稳定状态。禁止直接向 `main` 分支提交代码。
* `develop` 分ices分支：日常开发集成分支。
* `feature/*` 分支：新功能开发分支（如 `feature/card-system`, `feature/boss-taotie`）。
* `bugfix/*` 分支：修复线上或测试阶段的 Bug。

### 1.2 提交信息（Commit Message）格式
所有 Commit 必须采用规范化前缀：
* `feat:` 新增功能（如：`feat: 增加火+土熔岩淬炼的护甲转化逻辑`）
* `fix:` 修复 Bug（如：`fix: 修复烛九阴二阶段独眼大招不触发伤害的问题`）
* `refactor:` 代码重构（未改变功能，仅重构结构）
* `asset:` 纯美术、音效等资源的导入与更新
* `md:` 文档修改

### 1.3 忽略文件 (`.gitignore`)
请务必在本地或远程仓库根目录下配置 `.gitignore`，严禁提交以下文件：
```plaintext
.godot/
*.translation
.import/
export_presets.cfg

```

---

## 2. Godot 4 & GDScript 代码规范

为了保持代码的可读性，项目统一采用 Godot 官方推荐的 GDScript 风格指南。

### 2.1 命名规范

* **类与基类 (PascalCase)**：所有自定义类名、资源类名必须使用大驼峰。
```gdscript
class_name CardBase
extends Resource

```


* **文件与目录名 (snake_case)**：所有 `.gd` 脚本、`.tscn` 场景和资产文件名一律使用小写蛇形命名法。
* 正确：`combat_engine.gd`, `battle_scene.tscn`
* 错误：`CombatEngine.gd`, `BattleScene.tscn`


* **变量与函数名 (snake_case)**：
```gdscript
var current_mana: int = 0
func calculate_element_reaction() -> void:

```


* **常量名 (UPPERCASE_SNAKE_CASE)**：
```gdscript
const MAX_BOSS_ARMOR_LAYERS: int = 150

```



### 2.2 强类型声明（Strict Typing）

本项目强制开启强类型检查。所有变量、函数参数及返回值必须显式声明类型，禁止使用隐式 `variant`。

```gdscript
# 正确示例
@export var card_id: String = ""
@export var cost: int = 1

func apply_damage(amount: int, target: CharacterBase) -> bool:
	target.take_damage(amount)
	return target.is_dead()

```

### 2.3 节点引用规范

* 严禁在脚本中直接使用绝对路径引用节点（如 `$Panel/VBoxContainer/Label`），这会导致场景结构调整时代码崩溃。
* 必须统一使用 `@onready` 配合场景唯一节点（Scene Unique Nodes）或相对导出路径：

```gdscript
# 推荐使用场景唯一节点
@onready var boss_hp_bar: ProgressBar = %BossProgressBar 
@onready var intent_label: Label = %IntentLabel

```

---

## 3. 场景与资源配置规范

### 3.1 资源驱动设计 (Data-Driven)

* 卡牌与怪物属性严禁直接硬编码（Hard-coding）在脚本中。
* 30 张核心卡牌（如 `JK-001` 到 `TK-006`）必须全部通过继承自 `CardBase` 的自定义 Resource 进行实例化，存储在 `res://src/resources/cards/` 目录下，文件后缀为 `.tres`。

### 3.2 跨模块解耦（信号总线机制）

* 探索场景（瓦片地图层）与战斗场景（UI层）必须完全解耦。
* 两个场景之间的状态传递（如：在大地图上踩到僵尸、触发 Boss 战拉近镜头、击败 Boss 后返回大地图）必须全部通过全局单例 `SignalBus` 进行分发，禁止互相调用父子节点。

```gdscript
# 发送端：entities/enemies/boss_gusha.gd
func _on_player_collided():
	SignalBus.encounter_started.emit(self)

# 接收端：ui/combat_ui/battle_scene.gd
func _ready():
	SignalBus.encounter_started.connect(_on_battle_init)

```

---

## 4. UI 与特效开发规范

### 4.1 分辨率与适配

* 项目基准分辨率设置为 `1920x1080`。
* 所有 UI 节点（控制节点 Control）必须正确配置 **Anchors (锚点)** 和 **Containers (容器)**，确保在不同屏幕比例下布局不发生错位。

### 4.2 特效性能与分层

* 所有五行连锁反应、爆燃清场、惊天一剑等视觉特效，必须在 `BattleScene` 的“元素特效层”内独立渲染。
* 特效需使用 `GPUParticles2D` 实现以保证运行效率，且必须设置 `one_shot = true`，并在粒子播放完毕后自动释放内存，避免内存泄漏。

```

```